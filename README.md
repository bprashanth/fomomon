# fomomon

A photomon app for conservation. See [docs](./docs) for more..

Local build and run
```console 
$ flutter run --flavor {dev,alpha}
```
Admin UI
```
$ source .venv/bin/activate 
$ uvicorn admin.backend.main:app --reload --port 809
```
